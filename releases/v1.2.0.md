
# Release 1.2.0 (2026-03-02)

## Bug fixes

- **extract default credential to constant to satisfy CodeQL (#20)**
- **LTPA cookie extraction uses prefix matching for suffixed cookie names (#31)**
* fix: LTPA cookie extraction uses prefix matching for suffixed cookie names

Liberty's MQ REST API uses LtpaToken2_<suffix> cookie names by default. Changed from exact match to prefix match on LtpaToken2 and store the full cookie name for use in subsequent requests. Enabled the LTPA integration test.


## CI

- **add workflow to auto-add issues to GitHub Project**
- **add concurrency group to ci-push workflow (#44)**
Ensures new pushes cancel in-flight CI runs on the same branch.


## Documentation

- **add cross-repo documentation links to docs site (#33)**
Include the other-languages.md fragment from mq-rest-admin-common to show links to all language implementations on the docs home page.


## Features

- **initial template repository setup for Rust**
- **port Python examples to idiomatic Rust (#10)**
* feat(examples): port Python examples to idiomatic Rust

- **refactor examples to dual-mode with importable functions, tests, and coverage (#22)**
Extract example logic from 6 standalone binaries into a feature-gated src/examples.rs module with public functions, typed result structs, and 32 unit tests using MockTransport. Rewrite binaries as thin wrappers. Add 8 integration tests. Include examples in coverage (100% line coverage maintained).

- **auto-generate all MQSC command methods from mapping-data.json (#28)**
Replace hand-coded command methods with generated methods between BEGIN/END GENERATED MQSC METHODS markers. Add generation metadata to mapping-data.json. Add 4 new ALTER Q* methods (QLOCAL, QREMOTE, QALIAS, QMODEL). Move DELETE Q* methods from optional to required name pattern.

- **add SyncConfig construction validation (#43)**
* feat(sync): add SyncConfig construction validation


## Styling

- **rename abbreviated variables to descriptive PBP names (#12)**
Rename 16 abbreviated variable names across mapping.rs, session.rs, and mapping_merge.rs to comply with PBP naming conventions (mq-rest-admin-common#114). All changes are internal — no public API impact.


## Testing

- **add 570 tests achieving 100% line coverage (#14)**
* test: add 570 tests achieving 100% line coverage

Add comprehensive unit tests across all 11 source modules using inline #[cfg(test)] mod tests with a shared MockTransport in test_helpers.rs. Macro-generated tests cover all 148 command methods (success + error paths). Coverage threshold enforced at 100% in both scripts/dev/test.sh and CI workflow.

- **add 35 integration tests against live MQ queue managers (#18)**
- **enforce 100% region coverage (#24)**
Add targeted tests to exercise all previously missed LLVM regions and enforce --fail-under-regions 100 in both test.sh and CI. Replace assert!(matches!()) with Debug-format assertions to avoid unreachable pattern-match regions. Refactor two ? chains to .and_then() to resolve LLVM coverage instrumentation quirk.

- **add session state populated after command integration test (#40)**
* test: add session state populated after command integration test


## Revert

- **remove premature add-to-project workflow**
